# Google Tag Manager container ID (e.g. GTM-XXXXXXX).
# Create a container at https://tagmanager.google.com — GA4, Meta Pixel, and
# Google Ads tags are then configured inside that container, not in this repo.
# Public on purpose: GTM's container ID is visible in any page that uses it.
NEXT_PUBLIC_GTM_ID=
